export class Player {
  public name: string;
  public symbol: string;

  public constructor(name: string, symbol: string) {
    this.name = name;
    this.symbol = symbol;
  }
}

export default Player;
